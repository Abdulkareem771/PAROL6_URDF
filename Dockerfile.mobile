FROM parol6-ultimate:latest

# Install ROS bridge and web tools
RUN apt-get update && apt-get install -y \
    ros-humble-rosbridge-suite \
    ros-humble-web-video-server \
    python3-pip

RUN pip3 install tornado

WORKDIR /workspace
