# PAROL6 System — Final Handoff Audit

> Generated: 2026-03-03 | Pre-handoff review of all three major components

---

## 1. ROS Hardware Interface ([parol6_hardware/src/parol6_system.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp))

### ✅ Correct & Working
| Area | Detail |
|------|--------|
| **Lifecycle** | `on_init → on_configure → on_activate → read/write → on_deactivate → on_cleanup` chain is complete and correct |
| **Serial setup** | Port opened in [on_configure](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#103-150) at 115200 baud, 8N1, with 100ms VTIME timeout (non-blocking safe) |
| **State interfaces** | Exports [position](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/observer/AlphaBetaFilter.h#59-60) + [velocity](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/observer/AlphaBetaFilter.h#60-61) for all 6 joints |
| **Command interfaces** | Exports [position](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/observer/AlphaBetaFilter.h#59-60) + [velocity](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/observer/AlphaBetaFilter.h#60-61) commands for all 6 joints |
| **RX parsing** | Expects `<ACK,SEQ,J1_pos,J1_vel,...,J6_vel>` — 14 tokens — consistent with firmware `transport.send_feedback()` |
| **Bounds validation** | Encoder packets with any joint value outside URDF limits ±10% are **rejected** → falls back to echo-mode (prevents garbage from fake encoder poisoning planning) |
| **Kinematic inversion** | J1, J3, J6 cmd and feedback are multiplied by `-1.0` (matches `dir_invert` flags in legacy STM32 firmware [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp)) |
| **Spoof fallback** | Any parse failure / serial timeout / out-of-bounds packet echoes commands back as state so MoveIt doesn't time out |
| **Packet loss tracking** | Sequence counter with wraparound-safe loss detection; stats logged every 5 min |
| **Write framing** | `<SEQ,J1_pos,J1_vel,...,J6_vel>` — 13 values, sign-corrected before TX |

### ⚠️ Issues / Notes for Teammates
| Issue | Location | Action Required |
|-------|----------|-----------------|
| **Stale file header** | Lines 1–12 | Header still says "Day 1: SIL — no serial communication". The code now has **full serial communication active**. Update the comment. |
| **URDF limits hardcoded** | [read()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#261-449) ~line 388 | The `joint_lower/joint_upper` arrays must be kept in sync with the URDF joint limits. If the URDF is changed, update these arrays too. |
| **`goto spoof_states`** | [read()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#261-449) | Uses C `goto` for fallback. Valid and safe here, but note this for anyone unfamiliar with the pattern. |
| **`dir_signs` duplicated** | [read()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#261-449) and [write()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#460-498) | Same `{-1,1,-1,1,1,-1}` array declared twice. Extract to a class member or a file-scope constant to avoid divergence. |

---

## 2. Teensy Firmware ([parol6_firmware/src/main.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/main.cpp))

### ✅ Correct & Working
| Area | Detail |
|------|--------|
| **Config loading** | `#include "../generated/config.h"` with fallback defaults — clean separation between GUI and firmware |
| **Control loop** | 1 kHz `IntervalTimer` ISR: read encoder → alpha-beta filter → interpolator tick → P+FF control → velocity deadband → motor output |
| **Gains from GUI** | `KP_GAINS`, `MAX_VEL_RAD_S`, `VELOCITY_DEADBAND_RAD_S` all read from config.h generated by GUI |
| **Encoder HAL** | `QuadTimerEncoder` on Zone 1 pins: `[10,11,12,14,15,18]` — correct per migration guide |
| **Safety supervisor** | Feature-flagged; if disabled, direct motor passthrough (correct fallback) |
| **Telemetry** | `transport.send_feedback(seq, pos, vel)` at `FEEDBACK_RATE_HZ` — matches the `<ACK,SEQ,...>` format expected by [parol6_system.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp) |
| **Interpolator** | Adaptive delta_ms based on actual packet inter-arrival; capped at 100ms to prevent swing on packet drops |
| **Watchdog** | Feature-flagged, fed on each valid ROS command receipt |

### ⚠️ Issues / Notes for Teammates
| Issue | Location | Action Required |
|-------|----------|-----------------|
| **[set_motor_velocity()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/main.cpp#66-71) is a stub** | Line 67 | The function body is **empty** (`// motor_hal[axis].set_pwm(val)`). The FlexPWM stepper driver is not yet wired up. Real motor output will NOT work until this is implemented with `FlexPWMGenerator`. |
| **`dummy_stepper` is a placeholder** | Line 57 | `FlexPWMGenerator dummy_stepper(pin 2)` calls `init_dummy_carrier()` which likely just sets up a carrier without real STEP/DIR driving. Needs to be expanded to per-axis `FlexPWMGenerator` instances. |
| **DIR inversion not yet applied in firmware** | ISR, line 144 | [set_motor_velocity()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/main.cpp#66-71) doesn't apply `DIR_INVERT[]` from config.h yet. When the motor HAL is wired, apply `DIR_INVERT[i]` to flip sign before converting velocity to direction+step-frequency. |
| **ISR profiler print disabled** | Line 260 | `Serial.printf(...)` for ISR timing is commented out. Uncomment during bringup to validate 1 kHz budget. |

---

## 3. Firmware Configurator GUI (`parol6_firmware_configurator/`)

### ✅ Correct & Working
| Area | Detail |
|------|--------|
| **Joints tab** | Per-joint: STEP pin, DIR pin, Enc pin, Gear Ratio, Microstep, **Dir Inv (inversion checkbox)**, Max Vel, Max Current, Kp, Limit Type, Limit Pin, Limit Polarity |
| **`Dir Inv` checkbox** | Bound to `j.dir_invert` → written to `DIR_INVERT[6]` bool array in [config.h](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware_configurator/generated/config.h) → ready for the firmware to consume once motor HAL is wired |
| **Pin/Limit mapping** | All fields in the GUI map directly to `STEP_PINS`, `DIR_PINS`, `ENCODER_PINS`, `LIMIT_PINS`, `LIMIT_POLARITY`, `LIMIT_TYPE` in generated code — **complete and correct** |
| **Code generation** | [code_generator.py](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware_configurator/core/code_generator.py) generates a complete [config.h](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware_configurator/generated/config.h) covering feature flags, pins, gains, steps/rad, homing params, and a SHA-256 fingerprint |
| **Homing params** | `HOMING_ORDER`, `HOMING_SPEED`, `HOMED_OFFSET`, `STANDBY_POS` are included in config.h |
| **Saved profiles** | 10 phase configs exist (`phase0` through `phase10`) — ready to load and flash |

### ⚠️ Issues / Notes for Teammates
| Issue | Location | Action Required |
|-------|----------|-----------------|
| **GUI `Dir Inv` ≠ ROS `dir_signs`** | GUI → firmware vs. [parol6_system.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp) | These are **two separate inversion layers** that must be kept in sync. GUI inversion controls the **physical motor direction** (Teensy firmware). ROS `dir_signs` corrects the **kinematic sign** seen by MoveIt. Both should have J1, J3, J6 inverted. |
| **[phase0_hardware_check.json](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware_configurator/saved_configs/phase0_hardware_check.json)** | `saved_configs/` | Verify this profile has the correct Teensy 4.1 Zone 1 encoder pins `[10,11,12,14,15,18]` before flashing. |

---

## Quick Reference: Inversion Map

| Joint | Physical `Dir Inv` (GUI → Teensy) | ROS `dir_signs` (parol6_system.cpp) | Source |
|-------|------------------------------------|--------------------------------------|--------|
| J1 | ✅ Inverted | `-1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |
| J2 | Normal | `+1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |
| J3 | ✅ Inverted | `-1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |
| J4 | Normal | `+1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |
| J5 | Normal | `+1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |
| J6 | ✅ Inverted | `-1.0` | [motor_init.cpp](file:///home/kareem/Desktop/PAROL6_URDF/legacy%20code%20open%20loop/PAROL6%20control%20board%20test%20code/src/motor_init.cpp) legacy |

---

## Immediate Next Steps for Teammates

1. **Implement [set_motor_velocity()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/src/main.cpp#66-71)** with actual FlexPWM STEP/DIR output using `DIR_INVERT[]` from [config.h](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware_configurator/generated/config.h)
2. **Expand `FlexPWMGenerator`** from one dummy instance to 6 per-axis instances on STEP pins `[2,6,7,8,4,5]`
3. **Implement homing FSM** — `Group A (J4,J5,J6) first, then Group B (J1,J2,J3)` — J5 must partially move before J6 can home (mechanical coupling — see [LEGACY_CODE_MIGRATION_GUIDE.md](file:///home/kareem/Desktop/PAROL6_URDF/parol6_firmware/docs/LEGACY_CODE_MIGRATION_GUIDE.md))
4. **Update [parol6_system.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp) header** comment (still says "Day 1 SIL - No serial communication")
5. **Extract `dir_signs[]`** to a file-scope constant in [parol6_system.cpp](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp) so [read()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#261-449) and [write()](file:///home/kareem/Desktop/PAROL6_URDF/parol6_hardware/src/parol6_system.cpp#460-498) share one definition
