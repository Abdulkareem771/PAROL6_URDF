controller_manager:
  ros__parameters:
    update_rate: 25

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    parol6_arm_controller:
      type: joint_trajectory_controller/JointTrajectoryController

parol6_arm_controller:
  ros__parameters:
    joints:
      - joint_L1
      - joint_L2
      - joint_L3
      - joint_L4
      - joint_L5
      - joint_L6

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    allow_nonzero_velocity_at_trajectory_end: true

    constraints:
      stopped_velocity_tolerance: 0.01
      goal_time: 10.0  # Allow 10 seconds to reach goal
      joint_L1: {trajectory: 2.0, goal: 0.1}
      joint_L2: {trajectory: 2.0, goal: 0.1}
      joint_L3: {trajectory: 2.0, goal: 0.1}
      joint_L4: {trajectory: 2.0, goal: 0.1}
      joint_L5: {trajectory: 2.0, goal: 0.1}
      joint_L6: {trajectory: 2.0, goal: 0.1}
