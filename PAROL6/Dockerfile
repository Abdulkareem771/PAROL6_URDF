FROM ar4_gazebo:latest

# Set working directory to your PAROL6 workspace
WORKDIR /workspace

# Auto-source ROS (if not already in bashrc)
RUN echo "source /opt/ros/humble/setup.bash" >> /home/developer/.bashrc
